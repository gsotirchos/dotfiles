# Let xremap (running as a normal user) write synthetic input events.
# Grants the `input` group access to /dev/uinput; combined with adding the
# user to `input`, this avoids running xremap as root.
KERNEL=="uinput", GROUP="input", MODE="0660", OPTIONS+="static_node=uinput"
